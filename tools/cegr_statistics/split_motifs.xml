<tool id="split_motifs" name="Split motifs" version="1.0.0">
    <description></description>
    <requirements></requirements>
    <command>
        <![CDATA[
            mkdir -p output &&
            python $__tool_directory__/split_motifs.py
            --input "$input"
        ]]>
    </command>
    <inputs>
        <param name="input" type="data" format="tabular" label="Almost-gff input" />
    </inputs>
    <outputs>
        <collection name="motifs" type="list" label="Motifs: ${tool.name} on ${on_string}">
            <discover_datasets pattern="(?P&lt;designation&gt;.*)" directory="output" ext="gff" visible="false" />
        </collection>
    </outputs>
    <tests>
    </tests>
    <help>
**What it does**

Splits a Fimo-generated almost-gff file into a dataset collection consisting of a Gff file per motif contained within the input.

    </help>
    <expand macro="citations" />
</tool>